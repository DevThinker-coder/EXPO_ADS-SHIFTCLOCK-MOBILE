import { StyleSheet } from 'react-native';

import { Spacing } from 'styles';

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    top: 10,
    left: Spacing.vertical.size20,
  },
});

export default styles;
